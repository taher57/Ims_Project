import { FileFilterPipePipe } from './file-filter-pipe.pipe';

describe('FileFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FileFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
